# Public base URL of the deployed site — used for canonical tags, sitemap.xml,
# OG image URLs and JSON-LD. No trailing slash.
#
# On Vercel this is optional: VERCEL_PROJECT_PRODUCTION_URL is used automatically.
# Set it explicitly once you move to a custom domain.
NEXT_PUBLIC_SITE_URL=https://budget-bliss-nine.vercel.app

# Database — Neon / Postgres connection string
DATABASE_URL=

# Auth — Neon Auth (better-auth)
NEON_AUTH_BASE_URL=
NEON_AUTH_COOKIE_SECRET=
